# Tasks: 005-animations-and-footer

- [ ] 1. Alterar `Footer.tsx` para usar tons de azul claro/branco nos textos (`text-blue-100/70`, `text-white/80`, etc), resolvendo a falta de contraste.
- [ ] 2. Refatorar `Simulator.tsx` para importar `framer-motion` e utilizar `useScroll` / `useTransform`.
- [ ] 3. Implementar a lógica de `sticky top-0` no container principal do `Simulator` para travar a rolagem.
- [ ] 4. Aplicar a animação no `<motion.div>` de fundo, transformando-o de uma "onda curvada" para um fundo sólido completo (`borderRadius` de 100% para 0%).
- [ ] 5. Assegurar que a sessão posterior (`LeadForm`) tenha `z-index` e background opaco suficientes para realizar o efeito "cortina".
